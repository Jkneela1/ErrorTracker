@model IEnumerable<ErrorTracking.Models.Post>
@using PagedList.Mvc;


@{
    ViewData["Title"] = "Index";
}
<h1>Posts</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New Posts</a>
</p>
<form method="get" asp-action="Index">
    @*<p>
        <input type ="search" placeholder="Search For Posts.." value=" @ViewData["FindRelativePosts"]" name="Searching" style="width:200px;" />
        <input type="submit" value="Search" class="btn btn-primary" />
        
    </p>*@

    @using (Html.BeginForm())
    {
        <p>
            Find by name: @Html.TextBox("SearchString")
            <input type ="submit" value="Search" />
        </p>
    }

</form>

<div class="row">
    @foreach (var item in Model)
    {

        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    Post ID: @item.ID
                </div>
                <div class="card-body">
                    <h5 class="card-title">@item.PostName</h5>
                    <p class="card-text">
                        @{
                            var desc = item.PostDescription;
                        }
                        
                    </p>
                    <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-primary">Read More</a>
                </div>
            </div>
        </div>
                            }
    </div>


